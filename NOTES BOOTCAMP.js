Create a new directory calle day four

Create a new file in the directory called "index.js"

Init this  directory with git.

Create a new github Repo. 

Add Commit, and push your directory to github. 

Create a JSON file called "data.json"

Add an array of objects to your JSON file.

In "index.js" create a function that reads "data.json" 
synchronously and logs out the continue.

